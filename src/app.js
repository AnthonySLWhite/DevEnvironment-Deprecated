import '@babel/polyfill';
import 'dotenv/config';
console.log(`This is my ${process.env.MY_DATABASE_PASSWORD}`);

import './server/server';
const x = [5, 3, 5, 'Hello', 4, 8, 4, 6,];

const y = { 1: 1, };
